version: '3.8'
services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: kutcheri
      POSTGRES_PASSWORD: kutcheri
      POSTGRES_DB: kutcheri
    ports:
      - "5434:5432"
    volumes:
      - ./db/init:/docker-entrypoint-initdb.d

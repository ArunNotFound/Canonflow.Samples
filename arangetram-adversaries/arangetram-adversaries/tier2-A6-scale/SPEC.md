# A6-scale  [TIER 2 — real, but sequenced after Tier 1]

nothing has seen 500 tables. Area 6 benchmarks don't exist. Introspect a generated 500-table/5000-column schema; assert time bounds and O(n) normalization; no memory blowup.

Do NOT build before Tier 1 holds. Attacking this early is the meta-trap:
sieging the interesting wall while a load-bearing one stands unverified.

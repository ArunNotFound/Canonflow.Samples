# A9 · The Silent Schema  [discomforts the THESIS, not the parser]
Most enterprise schemas keep constraints in app code; the DB is nullable
VARCHARs. Against such a schema CanonFlow's harvest is nearly empty.
ADVERSARY: db/init/silent.sql — realistic order DB, almost no CHECKs.
PASS: CanonFlow reports its own low yield PROMINENTLY ("3 proofs from 40
columns; this schema keeps its rules elsewhere"), and NO marketing sentence
becomes false when pointed here. If a sentence breaks, the sentence changes,
not the schema. The honest low-yield report becomes the greenfield sales
artifact. This is the standing gate on all future marketing copy.

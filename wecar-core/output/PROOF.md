# CanonFlow Mathematical Proof Report

> **GENERATED BY:** CanonFlow Proof Engine
> **TIMESTAMP:** 2026-07-24 16:34:56 UTC
> **STATUS:** CERTIFIED

## Executive Summary
This document certifies the semantic translation fidelity between the database truth and the generated frontend contracts (TypeScript / OpenAPI).

## Detailed Field Proofs
### Table: `public.group_members`

| Column | DB Bound (Truth) | TS Fidelity | OpenAPI Fidelity |
|--------|------------------|-------------|------------------|
| `role` | `role IN ('ADMIN', 'MODERATOR', 'MEMBER')` | ✅ Exact | ✅ Exact |

### Table: `public.groups`

| Column | DB Bound (Truth) | TS Fidelity | OpenAPI Fidelity |
|--------|------------------|-------------|------------------|
| `name` | `(((length((name)::text) >= 1) AND (length((name)::text) <= 100)))` | ❌ Unsupported | ❌ Unsupported |

### Table: `public.messages`

| Column | DB Bound (Truth) | TS Fidelity | OpenAPI Fidelity |
|--------|------------------|-------------|------------------|
| `receiver_id` | `((receiver_id IS NOT NULL AND group_id IS NULL) OR (receiver_id IS NULL AND group_id IS NOT NULL))` | ❌ Unsupported | ❌ Unsupported |
| `group_id` | `((receiver_id IS NOT NULL AND group_id IS NULL) OR (receiver_id IS NULL AND group_id IS NOT NULL))` | ❌ Unsupported | ❌ Unsupported |
| `content` | `(((length(content) > 0) AND (length(content) <= 4000)))` | ❌ Unsupported | ❌ Unsupported |
| `message_type` | `message_type IN ('TEXT', 'IMAGE', 'VIDEO', 'AUDIO', 'FILE')` | ✅ Exact | ✅ Exact |
| `sent_at` | `(read_at IS NULL OR read_at >= sent_at)` | ❌ Unsupported | ❌ Unsupported |
| `read_at` | `(read_at IS NULL OR read_at >= sent_at)` | ❌ Unsupported | ❌ Unsupported |

### Table: `public.user_profiles`

| Column | DB Bound (Truth) | TS Fidelity | OpenAPI Fidelity |
|--------|------------------|-------------|------------------|
| `privacy_setting` | `privacy_setting IN ('EVERYONE', 'CONTACTS', 'NOBODY')` | ✅ Exact | ✅ Exact |

### Table: `public.users`

| Column | DB Bound (Truth) | TS Fidelity | OpenAPI Fidelity |
|--------|------------------|-------------|------------------|
| `username` | `((((length((username)::text) >= 3) AND (length((username)::text) <= 50))) AND (((username)::text ~ '^[a-zA-Z0-9_]+$'::text)))` | ❌ Unsupported | ❌ Unsupported |
| `phone_number` | `(((length((phone_number)::text) >= 10) AND (length((phone_number)::text) <= 15)))` | ❌ Unsupported | ❌ Unsupported |
| `status` | `status IN ('ACTIVE', 'INACTIVE', 'BANNED', 'DELETED')` | ✅ Exact | ✅ Exact |

## Metrics
- **Exact Translations:** 8
- **Approximate (Safe but Lossy):** 0
- **Unsupported (Manual Guard Required):** 16

### ⚠️ WARNING: Unsupported Constraints Detected
Some database constraints could not be translated into frontend validators. Your system is vulnerable to 500 errors if these bounds are breached. Backend middleware guards are strictly required.

---
*Mathematical proof generated by the Lattice-Bounded Semantic Engine.*
